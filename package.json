{
  "name": "alnuqta-media",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "validate:site": "node scripts/validate-site.mjs",
    "lint": "node --check scripts/inject-homepage-feed.mjs && node --check scripts/validate-site.mjs",
    "test": "npm run lint && npm run validate:site"
  }
}
